# Credenciais do R2 (Cloudflare) usadas por `main.py sync`.
# Copie este arquivo para `.env` e preencha com os valores reais.
# Nunca commite o `.env` (já está no .gitignore).

R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
